const DestructuringProp = ({ id, name }: { id: number, name: string }) => {
    return (
        <div>
            <h1>ID:{id}</h1>
            <p>Name:{name}</p>
        </div>
    )
}

export default DestructuringProp